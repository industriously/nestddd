model accounts {
  id         String   @id
  sub        String   @unique
  /// @format email
  email      String   @unique
  username   String
  created_at DateTime @default(now())
  updated_at DateTime @default(now())
  is_deleted Boolean  @default(false)
}
